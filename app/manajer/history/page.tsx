import React from "react";

const history = () => {
  return (
    <div>
      history transaction all -filter by year,month,day (range) -filter by
      chasier
    </div>
  );
};

export default history;
